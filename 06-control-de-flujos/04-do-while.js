// DO WHILE
// A diferencia del while, este ejecuta el código al menos una vez,
// incluso si la condición es falsa al principio

let numero = 0;

do {
  console.log("🌀 Número:", numero);
  numero++;
} while (numero < 3);

// Se ejecuta una vez y luego repite si la condición es verdadera
